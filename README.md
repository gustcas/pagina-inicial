# Landing Page Portfolio

Landing page profesional con `React + Vite + Tailwind CSS`, preparada para desplegar en Netlify.

## Scripts

```bash
npm install
npm run dev
npm run build
```

## Deploy en Netlify

- Build command: `npm run build`
- Publish directory: `dist`

El archivo `netlify.toml` ya deja esa configuración lista.
